<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".NaturalisationApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_app_foreground"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_app_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.NaturalisationTracker">

        <activity
            android:name=".ChatbotActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".QcmCiviqueActivity"
            android:exported="false" />
        <activity
            android:name=".EntretienPrepActivity"
            android:exported="false" />
        <activity
            android:name=".StatusSearchActivity"
            android:exported="false" />
        <activity
            android:name=".DecretsActivity"
            android:exported="false" />

        <receiver
            android:name=".AppBootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
